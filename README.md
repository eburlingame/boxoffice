# boxoffice: a movie selection app

## Running the app

This app was bootstrapped using `create-react-app`.

1. Clone the repo
2. Install the dependencies:
   - `npm install` or `yarn install`
3. Start the server:
   - `npm start` or `yarn start`
4. Go to `http://localhost:3000`
